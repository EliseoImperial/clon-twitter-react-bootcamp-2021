const tweetReducer = (state = [], action) => {
  switch (action.type) {
    case "SHOW_TWEETS":
      return state;

    case "NEW_TWEET":
      
    default:
      return state;
  }
};

export default tweetReducer;
